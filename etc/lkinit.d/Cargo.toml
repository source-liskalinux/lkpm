[package]
name = "init"
version = "0.1.0"
edition = "2024"
authors = ["Liska Linux Project"]
description = "Early initramfs init program for Liska Linux"

[dependencies]
# Add depedencies if needed

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
